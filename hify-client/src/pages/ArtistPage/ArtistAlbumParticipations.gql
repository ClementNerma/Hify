query ArtistAlbumParticipations($artistId: String!, $pagination: PaginationInput!) {
  artist(id: $artistId) {
    albumParticipations(pagination: $pagination) {
      nodes {
        ...AlbumCard
      }

      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}
