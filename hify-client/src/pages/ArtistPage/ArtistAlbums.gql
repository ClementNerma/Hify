query ArtistAlbums($artistId: String!, $pagination: PaginationInput!) {
  artist(id: $artistId) {
    id # for cache
    albums(pagination: $pagination) {
      nodes {
        ...AlbumCard
      }

      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}
