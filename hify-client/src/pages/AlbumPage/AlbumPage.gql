query AlbumPage($albumId: String!, $albumYearStrategy: AlbumYearStrategy!) {
  album(id: $albumId) {
    name
    year(strategy: $albumYearStrategy)
    genres {
      id
      name
    }
    albumArtists {
      id
      name
    }
    tracks {
      ...AudioTrack
    }
  }
}
