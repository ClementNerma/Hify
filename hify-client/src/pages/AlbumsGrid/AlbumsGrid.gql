query AlbumsGrid($pagination: PaginationInput!) {
    albums(pagination: $pagination) {
      edges {
        node {
          id
          name
          albumArtists {
            id
            name
          }
        }
      }
    }
}